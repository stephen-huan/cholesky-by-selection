\begin{tikzpicture}[scale=0.75]
  \begin{axis}[
    % width={0.8\linewidth},
    grid={major},
    % title={Time with increasing \( k \)
    %   (\( n = 100, m = 100, l = 1024, \nu = 1.5 \))},
    xlabel={\( k \)},
    ylabel={Time (seconds)},
    % legend entries={C\( k \)-NN, \( k \)-NN},
    % legend pos={north west},
  ]
  \addplot [very thick, orange]    table
    {figures/mnist/k_time_conditional-knn.csv};
  \addplot [very thick, lightblue] table
    {figures/mnist/k_time_scikit-knn.csv};
  \end{axis}
\end{tikzpicture}
