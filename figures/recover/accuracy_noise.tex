\begin{tikzpicture}[baseline, scale=0.75]
  \begin{axis}[
    grid={major},
    % title={Accuracy with increasing \( \sigma^2 \)
    %   (\( N = 1024, s = 32 \))},
    xlabel={Noise (\( \sigma^2 \))},
    ylabel={Accuracy (IOU)},
    legend entries={C\( k \)-NN, \( k \)-NN, corr., rand.},
    legend pos={north east},
    legend style={nodes={scale=0.85, transform shape}},
  ]
  \addplot [very thick, orange]    table
    {figures/recover/noise_score_cknn.csv};
  \addplot [very thick, lightblue] table
    {figures/recover/noise_score_knn.csv};
  \addplot [very thick, seagreen]  table
    {figures/recover/noise_score_corr.csv};
  \addplot [very thick, silver]    table
    {figures/recover/noise_score_random.csv};
  \end{axis}
\end{tikzpicture}
